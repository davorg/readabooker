#!/usr/bin/perl 

use strict;
use warnings;
use feature 'say';
use FindBin '$RealBin';
use lib "$RealBin/../lib";

use Booker::App;

my $app = Booker::App->new({ root => "$RealBin/.." });

$app->build;

